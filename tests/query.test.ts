/* import gql from "graphql-tag";
import { createServerTest } from "./helper"; */

/* describe("queries", () => {
    test("get pets returns a pet", () => {
        const {query} = createtestse({

        })
    })
});
 */
